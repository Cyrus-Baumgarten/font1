<%= form_for("temp", :url => temp_session_path) do |f| %>
	<%= f.text_field :password, placeholder: "RECIVE A CODE?"%>
  <%= f.text_field :email, placeholder: "EMAIL" %>
  <%= f.submit 'GET STARTED' %>
<% end %>